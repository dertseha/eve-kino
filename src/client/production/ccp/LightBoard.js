/**
The light board provides access to lighting controls

@module Client
@class LightBoard
*/
define([], function() {
  "use strict";

  var LightBoard = function(scene) {
    this.scene = scene;
  };

  return LightBoard;
});